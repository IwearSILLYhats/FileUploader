<div class="hiddenForm">
  <% if(locals.currentUser) { %>
    <form action="/folder/create" method="post">
      <input type="hidden" name="currentFolder" value="<%= currentFolder?.id %>" />
      <label for="folderName">Name</label>
      <input type="text" name="name" id="folderName" value="Untitled Folder" />
      <button type="reset">Cancel</button>
      <button type="submit">Submit</button>
    </form>
  <% } else { %>
    <p>You must be logged in to create a folder.</p>
  <% } %>
</div>
